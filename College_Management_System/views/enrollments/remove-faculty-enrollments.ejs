<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Withdraw Faculty from Course</title>
    <link href="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
    <div class="container mt-5">
        <h1 class="mb-4">Withdraw Faculty from Course</h1>
        <form action="/enrollments/withdraw/faculty/<%= course_id %>" method="POST">
            <div class="form-group">
                <label for="faculty_id">Select Faculty Member</label>
                <select class="form-control" name="faculty_id" id="faculty_id" required>
                    <% enrolledFaculty.forEach(faculty => { %>
                        <option value="<%= faculty.faculty_id %>">
                            <%= faculty.first_name %> <%= faculty.last_name %>
                        </option>
                    <% }); %>
                </select>
            </div>
            <button type="submit" class="btn btn-danger">Withdraw Faculty</button>
            <a href="/enrollments/list" class="btn btn-secondary">Back to Enrollments</a>
        </form>
    </div>
</body>
</html>
